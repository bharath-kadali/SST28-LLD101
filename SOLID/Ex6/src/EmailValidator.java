public class EmailValidator implements NotificationValidator {
    @Override
    public void validate(Notification n) {
        // basic email validation could be added; keep permissive to match prior behavior
    }
}
